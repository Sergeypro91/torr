'use client';

import React from 'react';
import { App, Aside, Main } from '@/components';

export default function Index() {
    return (
        <App>
            <Aside />
            <Main />
        </App>
    );
}
